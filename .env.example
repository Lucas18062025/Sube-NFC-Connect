# SUBE NFC Connect — variables de entorno
#
# La app NO usa API keys (código verificado: sin Gemini ni servicios remotos).
# Los secretos Android (SDK path, firmas) van en `local.properties`
# (untracked, ver .gitignore), nunca en Git.
#
# Plantilla por si se agrega un backend en el futuro:
# API_BASE_URL="https://tu-api.ejemplo/"
